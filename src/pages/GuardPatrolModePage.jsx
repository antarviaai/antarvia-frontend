import React from 'react';
import { Typography } from '@mui/material';

const GuardPatrolModePage = () => {
  return (
    <div>
      <Typography variant="h4" gutterBottom>
        Patrol Mode
      </Typography>
      <Typography>
        Placeholder page for guard patrol routes.
      </Typography>
    </div>
  );
};

export default GuardPatrolModePage;